.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Tauri signing keys — NEVER commit
*.key
*.key.pub

# Superpowers brainstorming sessions (local only)
.superpowers/

# VS Code workspace settings (optional — remove if you want to share them)
.vscode/
